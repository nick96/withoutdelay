- [ ] Move third party protos into root directory (e.g. `protos/`) so they're
  accessible by all
- [ ] Make python and go linting and test tasks generic and put in root (e.g. `ci/`)
    - They'll need to be parameterised
- [ ] Build docker image of all services and push to docker hub
    - [ ] Setup secrets management (vault?)
    