# Temporal

Temporal is a simple GRPC service for tagging a given temporal expression (e.g. "tomorrow") and converting them
to absolute date times.

## Temporal expressions

## API

For the strict definition of the API see the [service definition](./temporal.proto), however, here's a quick overview.
